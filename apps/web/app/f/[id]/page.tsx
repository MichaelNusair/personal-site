/**
 * Transcription editor/viewer page.
 *
 * TODO: Copy the full implementation from the GetVL repo:
 *   Source: apps/web/app/[locale]/(public)/f/[id]/page.tsx
 *
 * Adapt: remove i18n, tenant dependencies.
 */
export default function TranscriptionEditorPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-muted-foreground">Transcription editor page - migrated from GetVL /f/[id]</p>
    </div>
  );
}

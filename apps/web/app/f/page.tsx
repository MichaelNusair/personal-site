/**
 * Transcription upload page.
 *
 * TODO: Copy the full implementation from the GetVL repo:
 *   Source: apps/web/app/[locale]/(public)/f/page.tsx
 *
 * This page needs to be adapted:
 * - Remove next-intl/i18n dependencies (this app is English-only)
 * - Update API routes to use local /api/f/* endpoints
 * - Remove tenant-related imports
 * - Update auth to use local auth (or simplify to a Google sign-in gate)
 */
export default function TranscriptionUploadPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-muted-foreground">Transcription upload page - migrated from GetVL /f</p>
    </div>
  );
}
